# CLAUDE.md

@AGENTS.md

Keep this file minimal: only Claude Code-specific instructions — `@`-imports, slash commands, hooks, settings — belong here. Everything else (project context, conventions, workflow, the coding standard) lives in `AGENTS.md` and the `docs/*` it imports.
